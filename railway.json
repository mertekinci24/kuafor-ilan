{
  "deploy": {
    "startCommand": "python manage.py makemigrations authentication profiles jobs && python manage.py migrate && python manual_admin.py && gunicorn kuafor_ilan.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
